import 'package:flutter/material.dart';

class Focusscreen extends StatelessWidget {
  const Focusscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Focusscreen');
  }
}
